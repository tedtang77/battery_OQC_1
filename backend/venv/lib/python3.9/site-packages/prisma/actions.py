# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class BatteryCellActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BatteryCell]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await BatteryCell.prisma().query_raw(
            'SELECT * FROM BatteryCell WHERE id = $1',
            1103527590,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BatteryCell
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await BatteryCell.prisma().query_first(
            'SELECT * FROM BatteryCell WHERE serialNumber = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.BatteryCellCreateInput,
        include: Optional[types.BatteryCellInclude] = None
    ) -> _PrismaModelT:
        """Create a new BatteryCell record.

        Parameters
        ----------
        data
            BatteryCell record data
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The created BatteryCell record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BatteryCell record from just the required fields
        batterycell = await BatteryCell.prisma().create(
            data={
                # data to create a BatteryCell record
                'serialNumber': 'ggciceaie',
                'model': 'bbehjachib',
                'energy': 2035015474.36880,
                'capacity': 1508029952.48625,
                'voltage': 1062517886.26783,
                'imageFile': 'biabhbdai',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.BatteryCellCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BatteryCell records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BatteryCell record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await BatteryCell.prisma().create_many(
            data=[
                {
                    # data to create a BatteryCell record
                    'serialNumber': 'idghgaicb',
                    'model': 'fjfddhigg',
                    'energy': 790425851.211191,
                    'capacity': 1149758321.164428,
                    'voltage': 1388290519.164741,
                    'imageFile': 'bghffegacj',
                },
                {
                    # data to create a BatteryCell record
                    'serialNumber': 'bhghchehcc',
                    'model': 'dcgchcbbf',
                    'energy': 1343201072.67578,
                    'capacity': 744964398.196968,
                    'voltage': 1116175964.86147,
                    'imageFile': 'bdadaadhag',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.BatteryCellWhereUniqueInput,
        include: Optional[types.BatteryCellInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BatteryCell record.

        Parameters
        ----------
        where
            BatteryCell filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The deleted BatteryCell record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batterycell = await BatteryCell.prisma().delete(
            where={
                'id': 1686638315,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.BatteryCellWhereUniqueInput,
        include: Optional[types.BatteryCellInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BatteryCell record.

        Parameters
        ----------
        where
            BatteryCell filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The found BatteryCell record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batterycell = await BatteryCell.prisma().find_unique(
            where={
                'id': 2000430152,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.BatteryCellWhereUniqueInput,
        include: Optional[types.BatteryCellInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BatteryCell record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BatteryCell filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The found BatteryCell record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batterycell = await BatteryCell.prisma().find_unique_or_raise(
            where={
                'id': 1868141281,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
        include: Optional[types.BatteryCellInclude] = None,
        order: Optional[Union[types.BatteryCellOrderByInput, List[types.BatteryCellOrderByInput]]] = None,
        distinct: Optional[List[types.BatteryCellScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BatteryCell records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BatteryCell records returned
        skip
            Ignore the first N results
        where
            BatteryCell filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatteryCell model
        order
            Order the returned BatteryCell records by any field
        distinct
            Filter BatteryCell records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BatteryCell]
            The list of all BatteryCell records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BatteryCell records
        batterycells = await BatteryCell.prisma().find_many(take=10)

        # find the first 5 BatteryCell records ordered by the model field
        batterycells = await BatteryCell.prisma().find_many(
            take=5,
            order={
                'model': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
        include: Optional[types.BatteryCellInclude] = None,
        order: Optional[Union[types.BatteryCellOrderByInput, List[types.BatteryCellOrderByInput]]] = None,
        distinct: Optional[List[types.BatteryCellScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BatteryCell record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BatteryCell filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatteryCell model
        order
            Order the returned BatteryCell records by any field
        distinct
            Filter BatteryCell records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BatteryCell
            The first BatteryCell record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BatteryCell record ordered by the energy field
        batterycell = await BatteryCell.prisma().find_first(
            skip=1,
            order={
                'energy': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
        include: Optional[types.BatteryCellInclude] = None,
        order: Optional[Union[types.BatteryCellOrderByInput, List[types.BatteryCellOrderByInput]]] = None,
        distinct: Optional[List[types.BatteryCellScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BatteryCell record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BatteryCell filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatteryCell model
        order
            Order the returned BatteryCell records by any field
        distinct
            Filter BatteryCell records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BatteryCell
            The first BatteryCell record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BatteryCell record ordered by the capacity field
        batterycell = await BatteryCell.prisma().find_first_or_raise(
            skip=1,
            order={
                'capacity': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.BatteryCellUpdateInput,
        where: types.BatteryCellWhereUniqueInput,
        include: Optional[types.BatteryCellInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BatteryCell record.

        Parameters
        ----------
        data
            BatteryCell record data specifying what to update
        where
            BatteryCell filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The updated BatteryCell record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        batterycell = await BatteryCell.prisma().update(
            where={
                'id': 1860847622,
            },
            data={
                # data to update the BatteryCell record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.BatteryCellWhereUniqueInput,
        data: types.BatteryCellUpsertInput,
        include: Optional[types.BatteryCellInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BatteryCell filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BatteryCell model

        Returns
        -------
        prisma.models.BatteryCell
            The created or updated BatteryCell record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batterycell = await BatteryCell.prisma().upsert(
            where={
                'id': 1448521415,
            },
            data={
                'create': {
                    'id': 1448521415,
                    'serialNumber': 'bhghchehcc',
                    'model': 'dcgchcbbf',
                    'energy': 1343201072.67578,
                    'capacity': 744964398.196968,
                    'voltage': 1116175964.86147,
                    'imageFile': 'bdadaadhag',
                },
                'update': {
                    'serialNumber': 'bhghchehcc',
                    'model': 'dcgchcbbf',
                    'energy': 1343201072.67578,
                    'capacity': 744964398.196968,
                    'voltage': 1116175964.86147,
                    'imageFile': 'bdadaadhag',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.BatteryCellUpdateManyMutationInput,
        where: types.BatteryCellWhereInput,
    ) -> int:
        """Update multiple BatteryCell records

        Parameters
        ----------
        data
            BatteryCell data to update the selected BatteryCell records to
        where
            Filter to select the BatteryCell records to update

        Returns
        -------
        int
            The total number of BatteryCell records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BatteryCell records
        total = await BatteryCell.prisma().update_many(
            data={
                'voltage': 1628650740.124960
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BatteryCell records present in the database

        Parameters
        ----------
        select
            Select the BatteryCell fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BatteryCell filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BatteryCellCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BatteryCell.prisma().count()

        # results: prisma.types.BatteryCellCountAggregateOutput
        results = await BatteryCell.prisma().count(
            select={
                '_all': True,
                'imageFile': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.BatteryCellCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
    ) -> types.BatteryCellCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.BatteryCellCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatteryCellWhereInput] = None,
        cursor: Optional[types.BatteryCellWhereUniqueInput] = None,
    ) -> Union[int, types.BatteryCellCountAggregateOutput]:
        """Count the number of BatteryCell records present in the database

        Parameters
        ----------
        select
            Select the BatteryCell fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BatteryCell filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BatteryCellCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BatteryCell.prisma().count()

        # results: prisma.types.BatteryCellCountAggregateOutput
        results = await BatteryCell.prisma().count(
            select={
                '_all': True,
                'processedAt': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BatteryCellCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.BatteryCellWhereInput] = None
    ) -> int:
        """Delete multiple BatteryCell records.

        Parameters
        ----------
        where
            Optional BatteryCell filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BatteryCell records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BatteryCell records
        total = await BatteryCell.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.BatteryCellScalarFieldKeys'],
        *,
        where: Optional['types.BatteryCellWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BatteryCellAvgAggregateInput'] = None,
        sum: Optional['types.BatteryCellSumAggregateInput'] = None,
        min: Optional['types.BatteryCellMinAggregateInput'] = None,
        max: Optional['types.BatteryCellMaxAggregateInput'] = None,
        having: Optional['types.BatteryCellScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BatteryCellCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BatteryCellScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BatteryCellScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BatteryCellGroupByOutput']:
        """Group BatteryCell records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BatteryCell fields to group records by
        where
            BatteryCell filter to select records
        take
            Limit the maximum number of BatteryCell records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BatteryCellGroupByOutput]
            A list of dictionaries representing the BatteryCell record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BatteryCell records by createdAt values
        # and count how many records are in each group
        results = await BatteryCell.prisma().group_by(
            ['createdAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class BatchProcessActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.BatchProcess]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await BatchProcess.prisma().query_raw(
            'SELECT * FROM BatchProcess WHERE id = $1',
            835903122,
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.BatchProcess
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await BatchProcess.prisma().query_first(
            'SELECT * FROM BatchProcess WHERE batchName = $1',
            'hgdhbjhhj',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.BatchProcessCreateInput,
        include: Optional[types.BatchProcessInclude] = None
    ) -> _PrismaModelT:
        """Create a new BatchProcess record.

        Parameters
        ----------
        data
            BatchProcess record data
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The created BatchProcess record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a BatchProcess record from just the required fields
        batchprocess = await BatchProcess.prisma().create(
            data={
                # data to create a BatchProcess record
                'batchName': 'ecjjjfbae',
                'totalCells': 1775811865,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.BatchProcessCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple BatchProcess records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of BatchProcess record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await BatchProcess.prisma().create_many(
            data=[
                {
                    # data to create a BatchProcess record
                    'batchName': 'ijdbeffgg',
                    'totalCells': 995405759,
                },
                {
                    # data to create a BatchProcess record
                    'batchName': 'cbachdgfce',
                    'totalCells': 271520213,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.BatchProcessWhereUniqueInput,
        include: Optional[types.BatchProcessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single BatchProcess record.

        Parameters
        ----------
        where
            BatchProcess filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The deleted BatchProcess record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batchprocess = await BatchProcess.prisma().delete(
            where={
                'id': 456633834,
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.BatchProcessWhereUniqueInput,
        include: Optional[types.BatchProcessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique BatchProcess record.

        Parameters
        ----------
        where
            BatchProcess filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The found BatchProcess record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batchprocess = await BatchProcess.prisma().find_unique(
            where={
                'id': 2058258651,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.BatchProcessWhereUniqueInput,
        include: Optional[types.BatchProcessInclude] = None
    ) -> _PrismaModelT:
        """Find a unique BatchProcess record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            BatchProcess filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The found BatchProcess record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batchprocess = await BatchProcess.prisma().find_unique_or_raise(
            where={
                'id': 1583689592,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
        include: Optional[types.BatchProcessInclude] = None,
        order: Optional[Union[types.BatchProcessOrderByInput, List[types.BatchProcessOrderByInput]]] = None,
        distinct: Optional[List[types.BatchProcessScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple BatchProcess records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of BatchProcess records returned
        skip
            Ignore the first N results
        where
            BatchProcess filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatchProcess model
        order
            Order the returned BatchProcess records by any field
        distinct
            Filter BatchProcess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.BatchProcess]
            The list of all BatchProcess records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 BatchProcess records
        batchprocess = await BatchProcess.prisma().find_many(take=10)

        # find the first 5 BatchProcess records ordered by the totalCells field
        batchprocess = await BatchProcess.prisma().find_many(
            take=5,
            order={
                'totalCells': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
        include: Optional[types.BatchProcessInclude] = None,
        order: Optional[Union[types.BatchProcessOrderByInput, List[types.BatchProcessOrderByInput]]] = None,
        distinct: Optional[List[types.BatchProcessScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single BatchProcess record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BatchProcess filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatchProcess model
        order
            Order the returned BatchProcess records by any field
        distinct
            Filter BatchProcess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BatchProcess
            The first BatchProcess record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BatchProcess record ordered by the processedAt field
        batchprocess = await BatchProcess.prisma().find_first(
            skip=1,
            order={
                'processedAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
        include: Optional[types.BatchProcessInclude] = None,
        order: Optional[Union[types.BatchProcessOrderByInput, List[types.BatchProcessOrderByInput]]] = None,
        distinct: Optional[List[types.BatchProcessScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single BatchProcess record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            BatchProcess filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned BatchProcess model
        order
            Order the returned BatchProcess records by any field
        distinct
            Filter BatchProcess records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.BatchProcess
            The first BatchProcess record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second BatchProcess record ordered by the createdAt field
        batchprocess = await BatchProcess.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.BatchProcessUpdateInput,
        where: types.BatchProcessWhereUniqueInput,
        include: Optional[types.BatchProcessInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single BatchProcess record.

        Parameters
        ----------
        data
            BatchProcess record data specifying what to update
        where
            BatchProcess filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The updated BatchProcess record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        batchprocess = await BatchProcess.prisma().update(
            where={
                'id': 878442065,
            },
            data={
                # data to update the BatchProcess record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.BatchProcessWhereUniqueInput,
        data: types.BatchProcessUpsertInput,
        include: Optional[types.BatchProcessInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            BatchProcess filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned BatchProcess model

        Returns
        -------
        prisma.models.BatchProcess
            The created or updated BatchProcess record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        batchprocess = await BatchProcess.prisma().upsert(
            where={
                'id': 1675280054,
            },
            data={
                'create': {
                    'id': 1675280054,
                    'batchName': 'cbachdgfce',
                    'totalCells': 271520213,
                },
                'update': {
                    'batchName': 'cbachdgfce',
                    'totalCells': 271520213,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.BatchProcessUpdateManyMutationInput,
        where: types.BatchProcessWhereInput,
    ) -> int:
        """Update multiple BatchProcess records

        Parameters
        ----------
        data
            BatchProcess data to update the selected BatchProcess records to
        where
            Filter to select the BatchProcess records to update

        Returns
        -------
        int
            The total number of BatchProcess records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all BatchProcess records
        total = await BatchProcess.prisma().update_many(
            data={
                'id': 1627576247
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
    ) -> int:
        """Count the number of BatchProcess records present in the database

        Parameters
        ----------
        select
            Select the BatchProcess fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BatchProcess filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BatchProcessCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BatchProcess.prisma().count()

        # results: prisma.types.BatchProcessCountAggregateOutput
        results = await BatchProcess.prisma().count(
            select={
                '_all': True,
                'batchName': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.BatchProcessCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
    ) -> types.BatchProcessCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.BatchProcessCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.BatchProcessWhereInput] = None,
        cursor: Optional[types.BatchProcessWhereUniqueInput] = None,
    ) -> Union[int, types.BatchProcessCountAggregateOutput]:
        """Count the number of BatchProcess records present in the database

        Parameters
        ----------
        select
            Select the BatchProcess fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            BatchProcess filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.BatchProcessCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await BatchProcess.prisma().count()

        # results: prisma.types.BatchProcessCountAggregateOutput
        results = await BatchProcess.prisma().count(
            select={
                '_all': True,
                'totalCells': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.BatchProcessCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.BatchProcessWhereInput] = None
    ) -> int:
        """Delete multiple BatchProcess records.

        Parameters
        ----------
        where
            Optional BatchProcess filter to find the records to be deleted

        Returns
        -------
        int
            The total number of BatchProcess records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all BatchProcess records
        total = await BatchProcess.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.BatchProcessScalarFieldKeys'],
        *,
        where: Optional['types.BatchProcessWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.BatchProcessAvgAggregateInput'] = None,
        sum: Optional['types.BatchProcessSumAggregateInput'] = None,
        min: Optional['types.BatchProcessMinAggregateInput'] = None,
        max: Optional['types.BatchProcessMaxAggregateInput'] = None,
        having: Optional['types.BatchProcessScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.BatchProcessCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.BatchProcessScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.BatchProcessScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.BatchProcessGroupByOutput']:
        """Group BatchProcess records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar BatchProcess fields to group records by
        where
            BatchProcess filter to select records
        take
            Limit the maximum number of BatchProcess records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.BatchProcessGroupByOutput]
            A list of dictionaries representing the BatchProcess record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group BatchProcess records by processedAt values
        # and count how many records are in each group
        results = await BatchProcess.prisma().group_by(
            ['processedAt'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models